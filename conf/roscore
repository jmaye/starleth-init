# This configuration file contains the default parameters of the StarlETH
# roscore startup script. Make sure to restart the corresponding service for
# changed parameters to take effect.

# Set RUN=yes to start roscore at boot time.
RUN=yes

# Set the port to run the master on.
PORT=11311
